{
  "name": "loretech",
  "version": "0.1.1",
  "description": "Conversation materializer. Turns conversation context into living, enriched, shareable artifacts (Echoes).",
  "type": "module",
  "bin": {
    "loretech": "./bin/loretech.js"
  },
  "files": [
    "SKILL.md",
    "install.ts",
    "bin/",
    "src/",
    "references/"
  ],
  "keywords": ["skill", "research", "echo", "agent", "claude", "codex", "mcp"],
  "license": "MIT",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check src/ install.ts",
    "serve": "bun run install.ts serve"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/bun": "latest",
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  }
}
