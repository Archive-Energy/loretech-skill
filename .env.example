# =============================================================================
# loretech-skill — User Credentials (written to ~/.loretech/.env)
# =============================================================================
# These are NOT project env vars — they're stored in the user's home directory
# and read by the skill at runtime. The installer (`bunx loretech init`)
# prompts for these interactively and writes ~/.loretech/.env automatically.
#
# This file exists only as documentation. Do not commit a filled-in copy.
# =============================================================================

# ── OpenRouter (required) ────────────────────────────────────────────────────
# Powers inference for echo composition + observational memory.
# Sign up: https://openrouter.ai → Dashboard → Keys → Create Key
OPENROUTER_API_KEY=

# ── Exa (required) ──────────────────────────────────────────────────────────
# Powers deep research (searchAndContents) and Websets (structured datasets).
# Sign up: https://exa.ai → Dashboard → API Keys
EXA_API_KEY=

# ── Loretech API Key (auto-provisioned) ─────────────────────────────────────
# Issued automatically by `bunx loretech init` via POST /keys.
# If billing is enabled on the engine, you'll complete Stripe checkout first.
# Can also be set manually if you already have a key.
LORETECH_API_KEY=
